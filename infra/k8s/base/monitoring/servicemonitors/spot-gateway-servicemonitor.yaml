apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spot-gateway
  namespace: monitoring
  labels:
    release: prom
spec:
  namespaceSelector:
    matchNames:
      - spot
  selector:
    matchLabels:
      app: spot-gateway
  endpoints:
    - targetPort: 8080
      path: /actuator/prometheus

