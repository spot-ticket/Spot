apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: spot-order
  namespace: monitoring
  labels:
    release: prom
spec:
  namespaceSelector:
    matchNames:
      - spot
  selector:
    matchLabels:
      app: spot-order
  endpoints:
    - targetPort: 8082
      path: /actuator/prometheus
