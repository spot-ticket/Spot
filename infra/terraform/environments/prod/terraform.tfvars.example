# =============================================================================
# Prod 환경 변수 설정
# 사용법: 이 파일을 terraform.tfvars로 복사하여 사용
# =============================================================================

# 데이터베이스 (필수)
db_username = "spot"
db_password = "your_very_secure_password"

# 선택 사항 (기본값 사용 시 생략 가능)
# project     = "spot"
# environment = "prod"
# region      = "ap-northeast-2"
